{
  "name": "stellar-vote",
  "private": true,
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=stellar_vote && npm run build --workspace=frontend",
    "typecheck": "npm run typecheck --workspace=frontend"
  }
}